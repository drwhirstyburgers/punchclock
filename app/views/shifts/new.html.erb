<h1>Clock In</h1></br>
<%= form_for [@teacher, @new_shift] do |f| %>
  <%= f.hidden_field :clock_in, :value => Time.now %>
  <%= f.hidden_field :working, :value => true %>
  <div class="form-group">
    <%= f.submit "Clock In", class: 'btn btn-success' %>
  </div>
<% end %>
